#include <iostream>
#include <string>
#include <curl/curl.h>
#include <unistd.h>

using namespace std;

void pown(string url_orig, string name, string downdir){
cout << "Give a name to the pown: ";
getline(cin, name);
}
